"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { useState } from "react";
export function RecordActions({ editHref, endpoint, canEdit, canDelete }: { editHref: string; endpoint: string; canEdit: boolean; canDelete: boolean }) { const router = useRouter(); const [error, setError] = useState(""); async function remove() { if (!window.confirm("このレコードを削除しますか？")) return; const response = await fetch(endpoint, { method: "DELETE" }); if (!response.ok) { const result = await response.json(); return setError(result.message ?? "削除できませんでした。"); } router.push(endpoint.replace("/api", "").replace(/\/[^/]+$/, "")); router.refresh(); } return <div><div className="flex gap-2">{canEdit ? <Link className="secondary-button" href={editHref}>編集</Link> : null}{canDelete ? <button className="secondary-button border-red-200 text-red-600 hover:border-red-400 hover:text-red-700" onClick={remove}>削除</button> : null}</div>{error ? <p className="mt-2 text-xs text-red-600">{error}</p> : null}</div>; }
