import Link from "next/link";
import { redirect } from "next/navigation";
import { KanbanBoard } from "@/components/deals/kanban-board";
import { PageHeading } from "@/components/ui/page-heading";
import { getAuthContext } from "@/lib/auth";
import { ownerScope } from "@/lib/crm";
import { prisma } from "@/lib/prisma";
export default async function DealBoardPage({ searchParams }: { searchParams: Promise<{ pipeline?: string }> }) { const context = await getAuthContext(); if (!context) redirect("/login"); const params = await searchParams; const pipelines = await prisma.pipeline.findMany({ where: { organizationId: context.organization.id }, include: { stages: { orderBy: { sortOrder: "asc" } } }, orderBy: [{ isDefault: "desc" }, { createdAt: "asc" }] }); const pipeline = pipelines.find((p) => p.id === params.pipeline) ?? pipelines[0]; if (!pipeline) return null; const deals = await prisma.deal.findMany({ where: { organizationId: context.organization.id, pipelineId: pipeline.id, deletedAt: null, ...(await ownerScope(context)) }, include: { owner: { select: { name: true } } }, orderBy: { updatedAt: "desc" } }); const links = await prisma.objectAssociation.findMany({ where: { organizationId: context.organization.id, sourceObjectType: "DEAL", sourceObjectId: { in: deals.map((d) => d.id) }, targetObjectType: "COMPANY", isPrimary: true } }); const companies = await prisma.company.findMany({ where: { organizationId: context.organization.id, id: { in: links.map((l) => l.targetObjectId) } }, select: { id: true, name: true } }); const companyNames = new Map(companies.map((c) => [c.id, c.name])); const dealCompanies = new Map(links.map((l) => [l.sourceObjectId, companyNames.get(l.targetObjectId) ?? null])); const stages = pipeline.stages.map((stage) => ({ id: stage.id, name: stage.name, stageType: stage.stageType, probability: stage.probability, deals: deals.filter((d) => d.stageId === stage.id).map((d) => ({ id: d.id, name: d.name, amount: d.amount ? Number(d.amount) : null, expectedCloseDate: d.expectedCloseDate?.toISOString() ?? null, ownerName: d.owner?.name ?? "未設定", companyName: dealCompanies.get(d.id) ?? null, stageId: d.stageId })) })); return <div className="mx-auto max-w-[1800px]"><PageHeading eyebrow="Deal pipeline" title="商談カンバン" description={`${pipeline.name}の商談をドラッグ＆ドロップで更新できます。`} action={<div className="flex gap-2"><Link href="/deals" className="secondary-button">リスト表示</Link><Link href="/settings/pipelines" className="secondary-button">ステージ設定</Link></div>} /><form className="mb-5"><select className="text-field max-w-sm" name="pipeline" defaultValue={pipeline.id}>{pipelines.map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}</select><button className="secondary-button ml-2" type="submit">切り替え</button></form><KanbanBoard stages={stages} /></div>; }
